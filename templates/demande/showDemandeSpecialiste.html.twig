{% extends 'base.html.twig' %}
{% block body %}
<h1> Demande  </h1><br>
<a href="{{path("logout")}}" class="btn float-right">deconnexion</a><td>

<p>	Evoyer depuis :
{{ demande.date|date("F jS \\a\\t g:ia") }}</p><br>

<table class="table table-striped">
<tbody>
<th colspan="6"> 
<tr> 
<td>
<p>Initiale Patient :{{ demande.initialePatient }}</p>
<td rowspan="5">
<a href="{{path("showExamenCliniqueS",{"id":demande.id})}}" class="btn btn-success">Examen Clinique</a><br><br>
<a href="{{path('showExamenBioS',{'id':demande.id})}}" class="btn btn-success">Examen Biologique</a><br><br>
<a href="{{path('showExamenATCDS',{'id':demande.id})}}" class="btn btn-success">Examen ATCD</a><br><br>
<a href="{{path('showExamenRadioS',{'id':demande.id})}}" class="btn btn-success"> Examen Radiologique</a><br><br>

</td></tr>
<tr><td>
<p> Age: {{ demande.age }}</p></td></tr>
<tr><td>
<p>Explicatif de la demande :{{ demande.explicatif }}</p></td></tr>
<tr><td>
<p> Niveau d'urgence : {{demande.niveauUrganece }}</p></td></tr>
<tr><td>
<p> Etat: {{ demande.etat}}</p></td></tr><br><tr>
<td>
<a href="{{path("listReponseS",{"id":demande.id})}}" class="btn btn-success">Afficher mes réponses</a></td></tr>
<tr><td>
<a href="{{path("repondre",{"id":demande.id})}}"class="btn btn-success">Répondre</a></td></tr>
<tr><td>
<a href="{{path("listedemandeRens",{"id":demande.id})}}"class="btn btn-success">Demandes des renseinements</a></td></tr>
<tr><td><a href="/" class="btn btn-primary">Retour</a></td></tr>

</table>

{% endblock %}