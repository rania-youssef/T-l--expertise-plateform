{% extends 'base.html.twig' %}
{% block body %}
<a href="/logout" class="btn float-right">deconnexion</a><td><br><br> 
<h1> Mes réponses</h1>
{% if reponses %}
<table class="table table-striped">
<tbody> 
<tr>
 {% for reponse in reponses %}
 <thead>
 <td><p>{{reponse.date|date("F jS \\a\\t g:ia") }} </p></td>
<tbody>
<tr>
<td>
<p>
{{reponse.reponse}}</p>
<a href="{{path("deleteReponse",{"id":reponse.id,"idD":reponse.demande.id})}}" class="btn float-right">Supprimer</a><td><br><br> 
</td>
</tr><br>
{% endfor %}
</tbody>
</table>
{% else %}
Pas des réponses
{% endif %}

<a href="{{path("repondre",{"id":id})}}"class="btn btn-success">Ajouter réponse </a><br><br>
<a href="{{ path('showdemandeS',{"id":id}) }}"class="btn btn-success">Retour</a>
 
{% endblock %}